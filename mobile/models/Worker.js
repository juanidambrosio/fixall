class Worker {
    constructor(id, categories, workerName, workerAddress, priceRange) {
        this.id = id;
        this.categories = categories;
        this.workerName = workerName;
        this.workerAddress = workerAddress;
        this.priceRange  = priceRange;
    }
}

export default Worker;
